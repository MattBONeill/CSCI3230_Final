extends layout
block in_head
	script 

block content
	
	div(class="content")

		div(class="col-sm-offset-2 col-sm-8 signin")
			div(style="font-size:24px; text-align:center", class="col-sm-12") Welcome Back
			form(class="form-horizontal", role="form", action="/SignIn", method="post")
				div(class="form-group")
					div(class="col-sm-12")
						input(type="text", class="form-control", id="useremail", name="useremail", placeholder="Enter Username Or Email")
				div(class="form-group")
					div(class="col-sm-12")
						input(type="password", class="form-control", id="password", name="password", placeholder="Password")
				div(class="form-group")
					div(class="col-sm-12")
						button(type="submit", class="btn btn-default col-sm-12") Sign In
